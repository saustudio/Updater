<UserControl x:Class="Updater.Controls.NewsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Updater"
             xmlns:enums="clr-namespace:Updater.Enums"
             mc:Ignorable="d" 
             d:DesignHeight="279" d:DesignWidth="754" Width="509" Height="278">
    <Grid Width="509" Height="278">
        <Border CornerRadius="5">
            <Border.BorderBrush>
                <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                    <GradientStop Color="Black" Offset="0"/>
                    <GradientStop Color="White" Offset="1"/>
                </LinearGradientBrush>
            </Border.BorderBrush>
            <Border.Background>
                <ImageBrush ImageSource="/Dispel;component/Pictures/News.png"/>
            </Border.Background>
            <Grid>
                <Button x:Name="RecommendedButton"  Content="Перейти к новостям" Template="{DynamicResource SimpleTextButtonControlTemplate}" Cursor="Hand" Margin="185,248,160,15" Foreground="#FF919191" Click="RecommendedButton_Click"></Button>
                <StackPanel Orientation="Horizontal" Margin="0,10,0,214">
                    <RadioButton IsChecked="{Binding AllChecked}" x:Name="AllRadio" Content="Все" Template="{DynamicResource NewsRadioButtonControlTemplate}" Width="76" Cursor="Hand"/>
                    <RadioButton IsChecked="{Binding NewsChecked}" x:Name="NewsRadio" Content="Новости сервера" Template="{DynamicResource NewsRadioButtonControlTemplate}" Width="148.3" Cursor="Hand"/>
                    <RadioButton IsChecked="{Binding NotifyChecked}" x:Name="NotificationRadio" Content="Объявления" Template="{DynamicResource NewsRadioButtonControlTemplate}" Width="140.977" Cursor="Hand"/>
                    <RadioButton IsChecked="{Binding EventsChecked}" x:Name="EventsRadio" Content="Ивенты" Template="{DynamicResource NewsRadioButtonControlTemplate}" Width="88.337" Cursor="Hand"/>
                </StackPanel>
                <ListView Margin="0,69,1,35" Background="{x:Null}" HorizontalAlignment="Stretch"
                          ItemsSource="{Binding ShowingNews}"
                          HorizontalContentAlignment="Stretch" BorderBrush="{x:Null}" Foreground="{x:Null}"
                          ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListView.Resources>
                        <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" 
                                         Color="Transparent"/>
                    </ListView.Resources>

                    <ListView.ItemContainerStyle>
                        <Style TargetType="{x:Type ListViewItem}">
                            <Setter Property="Padding" Value="0"/>
                            <Setter Property="Margin" Value="0"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type ListViewItem}">
                                        <ContentPresenter />
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListView.ItemContainerStyle>

                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid  HorizontalAlignment="Stretch" Height="23" Margin="0,5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="150"/>
                                    <ColumnDefinition Width="270"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Border CornerRadius="5" Grid.Column="0" Margin="10,0" HorizontalAlignment="Left">

                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Type}" Value="{x:Static enums:NewsTypes.News}">
                                                    <Setter Property="Background">
                                                        <Setter.Value>
                                                            <LinearGradientBrush EndPoint="2,1" StartPoint="0,0">
                                                                <GradientStop Color="#FF48C6EF"/>
                                                                <GradientStop Color="#FF5757CF" Offset="0.566"/>
                                                            </LinearGradientBrush>
                                                        </Setter.Value>
                                                    </Setter>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Type}" Value="{x:Static enums:NewsTypes.Notifications}">
                                                    <Setter Property="Background">
                                                        <Setter.Value>
                                                            <LinearGradientBrush EndPoint="2,1" StartPoint="0,0">
                                                                <GradientStop Color="#FF9B23EA"/>
                                                                <GradientStop Color="#FF5757CF" Offset="0.566"/>
                                                            </LinearGradientBrush>
                                                        </Setter.Value>
                                                    </Setter>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Type}" Value="{x:Static enums:NewsTypes.Events}">
                                                    <Setter Property="Background">
                                                        <Setter.Value>
                                                            <LinearGradientBrush EndPoint="2,1" StartPoint="0,0">
                                                                <GradientStop Color="#FFFFD130"/>
                                                                <GradientStop Color="#FF816604" Offset="0.566"/>
                                                            </LinearGradientBrush>
                                                        </Setter.Value>
                                                    </Setter>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>


                                    <TextBlock FontFamily="Arial" TextAlignment="Center" Margin="10,3" Foreground="#FFF9F2F2" FontSize="14" FontWeight="Normal" TextWrapping="Wrap">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Type}" Value="{x:Static enums:NewsTypes.News}">
                                                        <Setter Property="Text" Value="Новости проекта"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Type}" Value="{x:Static enums:NewsTypes.Notifications}">
                                                        <Setter Property="Text"  Value="Объявления"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Type}" Value="{x:Static enums:NewsTypes.Events}">
                                                        <Setter Property="Text"  Value="Ивенты"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </Border>
                                <TextBlock FontFamily="Arial" Grid.Column="1" Text="{Binding Title}" VerticalAlignment="Center"  Foreground="#FFF9F2F2" FontSize="14" FontWeight="Light" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis"/>
                                <TextBlock Margin="10,0" FontFamily="Arial" Grid.Column="2" TextAlignment="Right" Text="{Binding Date}" VerticalAlignment="Center" Foreground="#999999" FontSize="14" FontWeight="Light"/>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </Border>
    </Grid>
</UserControl>
