<UserControl x:Class="Updater.Controls.ShopNewsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Updater"
             mc:Ignorable="d" 
             d:DesignHeight="279" d:DesignWidth="754" Width="361" Height="200">
    <Grid Width="361" Height="200">
        <Border CornerRadius="5">
            <Border.Background>
                <ImageBrush ImageSource="/Dispel;component/Pictures/Shop.png"/>
            </Border.Background>
            <Grid>
                <Label x:Name="ShopTitle" FontFamily="Arial" FontSize="14" Foreground="White" Content="Последние товары с аукциона" Margin="11,7,85,168" FontWeight="Light"></Label>
                <Button x:Name="RecommendedButton"  Content="Перейти" Template="{DynamicResource SimpleTextButtonControlTemplate}" Cursor="Hand" Margin="264,14,5,172" Foreground="#FFECECEC" Click="RecommendedButton_Click"></Button>
                <ListView Margin="-2,57,-2,6" Background="{x:Null}" HorizontalAlignment="Stretch"
                          ItemsSource="{Binding ShopItems}"
                          HorizontalContentAlignment="Stretch" BorderBrush="{x:Null}" Foreground="{x:Null}"
                          ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListView.Resources>
                        <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" 
                                         Color="Transparent"/>
                    </ListView.Resources>

                    <ListView.ItemContainerStyle>
                        <Style TargetType="{x:Type ListViewItem}">
                            <Setter Property="Padding" Value="0"/>
                            <Setter Property="Margin" Value="0"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type ListViewItem}">
                                        <ContentPresenter />
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListView.ItemContainerStyle>

                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid Background="#FF182029" HorizontalAlignment="Stretch" Height="38" Margin="0,2">
                                <Image Source="{Binding Image}" Margin="16,5,311,4" Width="22" Height="22"/>
                                <TextBlock FontFamily="Arial" Text="{Binding Title}" Margin="59,13,68,10" Foreground="#FFF9F2F2" FontSize="14" FontWeight="Light" TextWrapping="Wrap"/>
                                <TextBlock FontFamily="Arial" TextAlignment="Right" Text="{Binding Price}" Margin="282,13,10,10" Foreground="#FFFFE27D" FontSize="14" FontWeight="Light"/>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>

            
                </ListView>
            </Grid>
        </Border>
    </Grid>
</UserControl>
